# Space

![Space 예시](/img/attributes/attribute-space.png)

`[Space]`는 프로퍼티 사이에 여백을 추가합니다.

```shaderlab
[Space(8)] _DetailStrength ("Detail Strength", Range(0,1)) = 0.5
```

## 문법

```shaderlab
[Space]
[Space(pixels)]
```

그룹을 새로 만들 정도는 아니지만 시각적으로 끊어야 하는 값 사이에 사용합니다.
