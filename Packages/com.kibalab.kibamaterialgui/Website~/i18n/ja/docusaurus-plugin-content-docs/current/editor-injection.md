# エディター注入

エディター注入は、KIBAMaterialGUI インスペクターの決まった場所に追加 UI を挿入する機能です。

カスタムレンダラーが「プロパティ1行」を変える API なら、エディター注入は「インスペクター周辺の UI」を追加する API です。

## 使う例

- ツールバー下にプロジェクト用ボタンを追加
- 特定グループの先頭にプリセットボタンを配置
- プロパティ下にヒントを表示
- グループ右クリックメニューに項目を追加
- シェーダー作者向けの診断を表示

