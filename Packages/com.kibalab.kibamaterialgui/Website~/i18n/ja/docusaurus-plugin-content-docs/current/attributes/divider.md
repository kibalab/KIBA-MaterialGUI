# Divider

![Divider の例](/img/attributes/attribute-divider.png)

`[Divider]` はインスペクター内に区切り線を追加します。

```shaderlab
[Divider] _AdvancedValue ("Advanced Value", Float) = 0
```

同じグループ内で意味のまとまりが変わるときにだけ使うのがおすすめです。
